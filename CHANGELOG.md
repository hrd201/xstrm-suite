# CHANGELOG

## 0.1.0-dev
- 建立 `xstrm-suite` 独立项目骨架
- 引入 `xstrm` / `xstrm-admin`
- 接入 `emby2alist` 同源配置逻辑
- 验证镜像型 `.strm` 方案（简单 / 复杂样本）
- 建立 `runtime.yaml` 主配置
- 建立模板渲染与应用流程
- 建立 bootstrap / upgrade / repair / uninstall 脚本
- 建立 nginx test / reload 辅助脚本
